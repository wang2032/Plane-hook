version: '3.8'

services:
  plane-webhook:
    build: .
    container_name: plane-webhook
    restart: unless-stopped
    ports:
      - "5000:3000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    networks:
      - plane-network

networks:
  plane-network:
    driver: bridge
